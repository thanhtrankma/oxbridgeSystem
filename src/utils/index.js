import {
  formatAmount,
  getNumberOnly,
  formatCurrency,
  autoFormatDate,
  genarateQueryString,
  formatPhoneNumber,
  convertArrayAlpha,
  convertArrayStudent,
} from './format';
import common, {
  showToastSuccess,
  showToastError,
  useTranslation,
} from './common';
import {storage} from './storage';
import auth from './auth';

export {
  formatAmount,
  getNumberOnly,
  formatCurrency,
  autoFormatDate,
  genarateQueryString,
  formatPhoneNumber,
  showToastSuccess,
  showToastError,
  common,
  storage,
  useTranslation,
  auth,
  convertArrayAlpha,
  convertArrayStudent,
};
